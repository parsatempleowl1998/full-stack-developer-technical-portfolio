// 1. Declare a variable called `name` and assign your name to it.

var name = "John";
console.log(name);

// 2. Create a variable `age` and assign your age to it.

var age = 25;
console.log(age);

// 3. Create a variable `isStudent` and assign it a boolean value.

var isStudent = true;
console.log(isStudent);

// 4. Create a variable `favoriteFruits` and assign it an array of strings representing your favorite fruits.

var favoriteFruits = ["apple", "banana", "orange"];
console.log(favoriteFruits);

// 5. Declare a variable `isSingleDigit` and assign it a boolean value based on the value of the given number, `num` if its less than 10

var num = 8;
var isSingleDigit = num < 10;
console.log(isSingleDigit);

// 6. Create a variable `myNullValue` and assign it a null value.

var myNullValue = null;
console.log(myNullValue);

// 8. Create a variable `undefinedVariable` without assigning any value to it.

var undefinedVariable;
console.log(undefinedVariable);

// 9. Declare a variable `ageString` and assign it a numeric value converted to a string.

var ageString = String(25);
console.log(ageString);

// 10. Create a variable `isBoolean` and assign it the boolean value of whether a given number, `num`, is greater than 10.

var num = 15;
var isBoolean = num > 10;
console.log(isBoolean);
